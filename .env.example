# ComparaAI NestJS backend'inin adresi (lib/api.ts tarafından kullanılır)
NEXT_PUBLIC_API_URL=http://localhost:3001

# NOT: Vercel'e deploy edilince (Faz 8) bu değer, backend'in production
# URL'i ile (örn. https://api.comparaai.com) Vercel proje ayarlarından
# environment variable olarak set edilecek.
